<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext is off everywhere. A media app that permits http:// invites a
  network attacker to swap the video stream, and every backend and storage
  endpoint this app talks to is https.

  The debug overrides exist so a developer can put a proxy in front of the app
  and have its certificate trusted. They apply to a *debuggable* build only —
  and the debug build type is no longer debuggable, because that is what gets
  installed on a phone and a debuggable APK is never properly compiled. Flip
  `isDebuggable` back in build.gradle.kts for the length of a session that
  needs them.

  The cleartext exception for a machine on the other end of the emulator is a
  `domain-config`, so it is unaffected by any of that and applies everywhere.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
