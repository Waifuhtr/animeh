<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext is off everywhere. A media app that permits http:// invites a
  network attacker to swap the video stream, and every backend and storage
  endpoint this app talks to is https.

  The debug overrides exist so a developer can point the app at a WordPress
  running on their own machine; they apply to the debug build only, because
  `debug-overrides` is ignored in a release APK.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
